{
  "name": "vibeos",
  "version": "0.1.0",
  "private": true,
  "description": "VibeOs — AI operating system: macOS-style desktop where apps are hallucinated HTML generated on the fly by Claude Code.",
  "workspaces": [
    "shell",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n shell,server -c cyan,magenta \"npm:dev:shell\" \"npm:dev:server\"",
    "dev:shell": "npm run dev --workspace=shell",
    "dev:server": "npm run dev --workspace=server",
    "build": "npm run build --workspace=shell && npm run build --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
